package info.ralab.uxdf.definition;

/**
 * Sd定义权限描述
 */
public class SdAuthority {

    /**
     * 当前Sd定义是否不可新建
     */
    private boolean createDisabled = false;

    /**
     * 当前Sd定义是否不可编辑
     */
    private boolean editDisabled = false;

    /**
     * 当前Sd定义是否不可删除
     */
    private boolean deleteDisabled = false;
}
